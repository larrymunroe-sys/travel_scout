services:
  - type: web
    name: travel-scout
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: bash run.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
